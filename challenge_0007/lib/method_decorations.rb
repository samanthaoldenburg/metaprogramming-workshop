module MethodDecorations
  # Makes a method class-level
  def static(method_name)
  end

  # Saves the method's output to a constant and recalls that constant on subsequent calls
  def const(method_name)
  end
end
